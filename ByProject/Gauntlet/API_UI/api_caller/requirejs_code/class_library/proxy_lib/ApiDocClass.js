
//Defines the handles for the API so that we can use the api_caller:
var ApiDocClass = function(){
	
	
	//Can we make a global container?
	
	/*
	var service_handles  = new RegistryOfServiceHandleIDs();
	var argument_handles = new RegistryOfArgNameHandleIDs();
	
	//Object wiring up SUBMIT TOKEN api call:
	//////////////////////////////////////////////////////////////////////////////
	this.SUBMIT_TOKEN = new HandleForServiceCall();
	this.SUBMIT_TOKEN.id = service_handles.SUBMIT_TOKEN;
	this.SUBMIT_TOKEN.http_method = "GET";
	this.SUBMIT_TOKEN.about = "submits a token for validation";
	
	this.SUBMIT_TOKEN.TOKEN_HASH      = new HandleForArgument();
	this.SUBMIT_TOKEN.TOKEN_HASH.id   = argument_handles.TOKEN_HASH;
	this.SUBMIT_TOKEN.TOKEN_HASH.type = String;
	this.SUBMIT_TOKEN.TOKEN_HASH.about= "The string representation of a token";
	
	
	//////////////////////////////////////////////////////////////////////////////
	*/
	
}//CLASS::END